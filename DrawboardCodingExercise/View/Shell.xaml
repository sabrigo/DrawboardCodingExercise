<Page
    x:Class="DrawboardCodingExercise.View.Shell"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:valueConverters="using:DrawboardCodingExercise.ValueConverters"
    xmlns:designTime="using:DrawboardCodingExercise.ViewModel.DesignTime"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    d:DataContext="{d:DesignInstance designTime:DesignTimeShellViewModel, IsDesignTimeCreatable=True}">

	<Page.Resources>
		<valueConverters:PageHeaderValueConverter x:Key="PageHeaderValueConverter" />
		<valueConverters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
	</Page.Resources>

	<RelativePanel>
		<Border x:Name="TitleBar"
                RelativePanel.AlignTopWithPanel="True"
                RelativePanel.AlignLeftWithPanel="True"
                RelativePanel.AlignRightWithPanel="True"
                Background="{ThemeResource SystemControlAcrylicWindowBrush}">
			<Grid Height="40" Margin="24,0">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="Auto"/>
					<ColumnDefinition Width="Auto" />
					<ColumnDefinition />
					<ColumnDefinition Width="Auto" />
				</Grid.ColumnDefinitions>
				<Button Margin="0,0,8,0" Visibility="{Binding CanGoBack}" Command="{Binding GoBackCommand}">
					<SymbolIcon Symbol="Back" />
				</Button>
				<TextBlock
                    Text="{Binding ElementName=NavFrame, Path=Content.DataContext, Converter={StaticResource PageHeaderValueConverter}, FallbackValue=Bleah}"
					VerticalAlignment="Center"
                    Style="{StaticResource TitleTextBlockStyle}" Grid.Column="1" />
				<StackPanel Grid.Column="3" Orientation="Horizontal"
                            Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}">

					<TextBlock VerticalAlignment="Center" Text="{Binding ThingInProgress}" />
					<ProgressRing IsActive="{Binding IsBusy}"
                                  Margin="8,0"/>
				</StackPanel>
			</Grid>
		</Border>
		<Frame x:Name="NavFrame"
               RelativePanel.Below="TitleBar"
               RelativePanel.AlignLeftWithPanel="True"
               RelativePanel.AlignRightWithPanel="True"
               RelativePanel.AlignBottomWithPanel="True" />
	</RelativePanel>
</Page>