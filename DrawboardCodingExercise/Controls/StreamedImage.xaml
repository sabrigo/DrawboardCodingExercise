<UserControl
    x:Class="DrawboardCodingExercise.Controls.StreamedImage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="64"
    d:DesignWidth="64">

    <Grid>
        <SymbolIcon x:Name="ErrorIcon" Symbol="Cancel" Visibility="Collapsed" Foreground="{Binding ErrorBrush, RelativeSource={RelativeSource TemplatedParent}}" Width="{x:Bind Width}" Height="{x:Bind Height}" />
        <Image x:Name="PresentedImage" Width="{x:Bind Width}" Height="{x:Bind Height}" />
        <ProgressRing x:Name="ProgressRing" Width="{x:Bind Width}" Height="{x:Bind Height}" />
    </Grid>
</UserControl>
